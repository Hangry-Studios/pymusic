[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "pymusic"
version = "0.1.0"
description = "Make music with Python code"
readme = "README.md"
license = {text = "MIT"}
requires-python = ">=3.9"
dependencies = []
